const SUPABASE_URL="https://ywwojkvarygebbfwiymq.supabase.co";const SUPABASE_ANON_KEY="sb_publishable_uUqW1tX4WdY1we6yj5PnuQ_KpYe_M17";const db=supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY,{auth:{persistSession:true,autoRefreshToken:true}});const H12=12*60*60*1000;function remember(){localStorage.setItem("bb_exp",Date.now()+H12)}async function logoutUser(path="login.html"){await db.auth.signOut();localStorage.removeItem("bb_exp");location.href=path}async function media(file,folder){if(!file)return null;const p=folder+"/"+Date.now()+"_"+file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const{error}=await db.storage.from("business-media").upload(p,file);if(error)throw error;return db.storage.from("business-media").getPublicUrl(p).data.publicUrl}