function ar(){let d=db();adminStats.innerHTML=`<div class=metric>Users<strong>${d.users.length}</strong></div><div class=metric>Businesses<strong>${d.businesses.length}</strong></div><div class=metric>Products<strong>${d.products.length}</strong></div><div class=metric>Services<strong>${d.services.length}</strong></div>`;let u=document.querySelector('#users');if(u)u.innerHTML=d.users.map(x=>`<tr><td>${x.name}</td><td>${x.email}</td><td>${x.blocked?'Blocked':'Active'}</td><td><button class=btn onclick="block(${x.id})">${x.blocked?'Unblock':'Block'}</button><button class='btn danger' onclick="du(${x.id})">Delete</button></td></tr>`).join('');let b=document.querySelector('#businesses');if(b)b.innerHTML=d.businesses.map(x=>`<tr><td>${x.name}</td><td>${x.category}</td><td>${x.status}</td><td><button class=btn onclick="toggle(${x.id})">Activate/Deactivate</button><button class='btn danger' onclick="dbiz(${x.id})">Delete</button></td></tr>`).join('')}function block(i){let d=db(),x=d.users.find(x=>x.id==i);x.blocked=!x.blocked;save(d);ar()}function du(i){let d=db();d.users=d.users.filter(x=>x.id!=i);save(d);ar()}function toggle(i){let d=db(),x=d.businesses.find(x=>x.id==i);x.status=x.status=='Active'?'Inactive':'Active';save(d);ar()}function dbiz(i){let d=db();d.businesses=d.businesses.filter(x=>x.id!=i);save(d);ar()}document.addEventListener('DOMContentLoaded',ar)