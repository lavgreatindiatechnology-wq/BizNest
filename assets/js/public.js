const sb=window.supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY);const slug=new URLSearchParams(location.search).get('slug');function e(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
(async()=>{if(!slug){loading.innerHTML='<div class="card"><h2>Business website not found</h2><p>Create your business first.</p></div>';return}const {data:b,error}=await sb.from('businesses').select('*').eq('slug',slug).maybeSingle();if(error||!b){loading.innerHTML='<div class="card"><h2>Business website not found</h2></div>';return}brand.textContent=b.name||'Business';businessName.textContent=b.name||'Business';tagline.textContent=b.tagline||'';contact.textContent=[b.phone,b.email].filter(Boolean).join(' • ');address.textContent=b.address||'';customerLink.href=new URL('customer.html?slug='+encodeURIComponent(slug),location.href).href;const {data:p}=await sb.from('products').select('*').eq('business_id',b.id).eq('active',true);productGrid.innerHTML=(p||[]).map(x=>`<div class="card"><img src="${e(x.image_url||'https://placehold.co/500x300?text=Product')}" style="width:100%;height:150px;object-fit:contain"><h3>${e(x.name)}</h3><p>${e(x.description)}</p><b>₹${e(x.price||0)}</b><br><br><a class="btn orange" href="${customerLink.href}">Order Now</a></div>`).join('')||'<p>No products added yet.</p>';const {data:s}=await sb.from('services').select('*').eq('business_id',b.id).eq('active',true);serviceGrid.innerHTML=(s||[]).map(x=>`<div class="card"><h3>🛠️ ${e(x.name)}</h3><p>${e(x.description)}</p><b>₹${e(x.price||0)}</b><br><br><a class="btn orange" href="${customerLink.href}">Book Appointment</a></div>`).join('')||'<p>No services added yet.</p>';loading.classList.add('hidden');content.classList.remove('hidden')})();
