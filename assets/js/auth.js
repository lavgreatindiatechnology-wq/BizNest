document.querySelector("#registerForm")?.addEventListener("submit",e=>{e.preventDefault();let d=db(),u={id:Date.now(),name:name.value,email:email.value,password:password.value,blocked:false};if(d.users.some(x=>x.email===u.email))return msg.textContent="Email already registered";d.users.push(u);save(d);localStorage.setItem("biznest_session",JSON.stringify(u));location="app/onboarding.html"});document.querySelector("#loginForm")?.addEventListener("submit",e=>{e.preventDefault();let u=db().users.find(x=>x.email===email.value&&x.password===password.value);if(!u)return msg.textContent="Invalid login";if(u.blocked)return msg.textContent="Account blocked";localStorage.setItem("biznest_session",JSON.stringify(u));location="app/dashboard.html"});