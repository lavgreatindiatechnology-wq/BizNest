const sb=supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY);const isReg=location.pathname.endsWith('register.html');const msg=document.getElementById('msg');function say(t,e=false){msg.textContent=t;msg.style.color=e?'#b42318':'#067647'}document.getElementById('go').onclick=async()=>{const email=document.getElementById('email').value.trim(),password=document.getElementById('password').value;if(!email||!password)return say('Please enter email and password.',true);try{if(isReg){const name=document.getElementById('name').value.trim();if(!name)return say('Please enter owner name.',true);const {data,error}=await sb.auth.signUp({email,password,options:{data:{name}}});if(error)throw error;if(data.session){say('Account created. Opening dashboard...');setTimeout(()=>location.href='dashboard.html',500)}else say('Account created. Please confirm your email, then login.')}else{const {error}=await sb.auth.signInWithPassword({email,password});if(error)throw error;say('Login successful. Opening dashboard...');setTimeout(()=>location.href='dashboard.html',350)}}catch(e){say(e.message,true)}};