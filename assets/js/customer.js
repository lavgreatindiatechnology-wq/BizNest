const sb=window.supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY);const slug=new URLSearchParams(location.search).get('slug')||'';const siteURL=slug?new URL('site.html?slug='+encodeURIComponent(slug),location.href).href:new URL('index.html',location.href).href;back.href=siteURL;continueBtn.onclick=()=>location.href=siteURL;
async function refresh(){const {data:{session}}=await sb.auth.getSession();authBox.classList.toggle('hidden',!!session);loggedBox.classList.toggle('hidden',!session)}
loginBtn.onclick=async()=>{msg.textContent='Logging in...';const {error}=await sb.auth.signInWithPassword({email:email.value.trim(),password:password.value});if(error){msg.textContent=error.message;return}location.href=siteURL};
registerBtn.onclick=async()=>{const {error}=await sb.auth.signUp({email:email.value.trim(),password:password.value});msg.textContent=error?error.message:'Account created. Please login.'};logoutBtn.onclick=async()=>{await sb.auth.signOut();refresh()};refresh();
